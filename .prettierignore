dist
.tsbuild
coverage
pnpm-lock.yaml

# The documentation is settled. Prettier would reflow it — line breaks, tables,
# lists — which is rewriting content rather than formatting it. It stays under
# human control.
*.md
docs/

# Written by `prisma generate`. Formatting it is undone on the next run, and
# the diff it leaves is noise in the one file that is meant to be read as a diff.
examples/demo/src/generated/
