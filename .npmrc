# An undeclared dependency must not resolve. That is what stops a package
# importing what it never declared, and with it the architecture boundaries of
# ARCH 12 §1.
strict-peer-dependencies=false
auto-install-peers=true
resolution-mode=highest
