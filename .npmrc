# Les dépendances non déclarées ne doivent pas être résolvables : c'est ce qui
# empêche un paquet d'importer ce qu'il n'a pas déclaré, et donc de contourner
# les frontières de l'ADR 12.
strict-peer-dependencies=false
auto-install-peers=true
resolution-mode=highest
