---
"@perchjs/core": minor
---

Judge the value at the trust boundary, not only the path.

Stage 5 asked whose a path was and never what it carried, so anything a client
was allowed to touch at all it could write anything into: an object, a list
where one value belongs, or a value no declaration ever named. A declared
option list is now the closed set it was always described as.
